(function(){
const canvas=document.getElementById('globo-canvas');if(!canvas||typeof THREE==='undefined')return;const hero=document.getElementById('globo-hero'),dica=document.getElementById('globo-dica');try{const scene=new THREE.Scene();const renderer=new THREE.WebGLRenderer({canvas,antialias:true,alpha:true});let w=hero.clientWidth,h=hero.clientHeight;renderer.setSize(w,h);renderer.setPixelRatio(Math.min(window.devicePixelRatio,2));const camera=new THREE.PerspectiveCamera(42,w/h,.1,100);camera.position.z=3.25;const group=new THREE.Group();scene.add(group);const globe=new THREE.Mesh(new THREE.SphereGeometry(1,64,64),new THREE.MeshPhongMaterial({color:0x183a32,transparent:true,opacity:.96,shininess:5}));group.add(globe);scene.add(new THREE.AmbientLight(0xffffff,1.2));const light=new THREE.DirectionalLight(0xffffff,.8);light.position.set(4,3,5);scene.add(light);function latLon(lat,lon,r=1.02){const phi=(90-lat)*Math.PI/180,theta=(lon+180)*Math.PI/180;return new THREE.Vector3(-r*Math.sin(phi)*Math.cos(theta),r*Math.cos(phi),r*Math.sin(phi)*Math.sin(theta))}const markers=[];OBRAS.forEach(o=>{const l=(o.lugares||[])[0];if(!l)return;const z=CERTEZAS[l.certeza]||CERTEZAS.ilustrativo;const m=new THREE.Mesh(new THREE.SphereGeometry(z.tracejado?.014:.022,12,12),new THREE.MeshBasicMaterial({color:0xc45132,transparent:true,opacity:z.tracejado?.55:1}));m.position.copy(latLon(l.lat,l.lon));m.userData.obra=o;group.add(m);markers.push(m)});let dragging=false,lastX=0,rot=-.6;canvas.addEventListener('mousedown',e=>{dragging=true;lastX=e.clientX});window.addEventListener('mousemove',e=>{if(dragging){rot+=(e.clientX-lastX)*.005;lastX=e.clientX}});window.addEventListener('mouseup',()=>dragging=false);const ray=new THREE.Raycaster(),pointer=new THREE.Vector2();function setPointer(e){const r=canvas.getBoundingClientRect();pointer.x=((e.clientX-r.left)/r.width)*2-1;pointer.y=-((e.clientY-r.top)/r.height)*2+1;ray.setFromCamera(pointer,camera)}canvas.addEventListener('mousemove',e=>{if(dragging)return;setPointer(e);const hit=ray.intersectObjects(markers)[0];if(hit&&dica){const o=hit.object.userData.obra;dica.textContent=`${o.titulo} — ${o.autor}`;dica.style.opacity='1';canvas.style.cursor='pointer'}else if(dica){dica.style.opacity='0';canvas.style.cursor='grab'}});canvas.addEventListener('click',e=>{setPointer(e);const hit=ray.intersectObjects(markers)[0];if(hit)location.href=`obra.html?id=${hit.object.userData.obra.id}`});function loop(){requestAnimationFrame(loop);if(!dragging)rot+=.0007;group.rotation.y=rot;renderer.render(scene,camera)}loop();window.addEventListener('resize',()=>{w=hero.clientWidth;h=hero.clientHeight;camera.aspect=w/h;camera.updateProjectionMatrix();renderer.setSize(w,h)})}catch(e){hero.classList.add('sem-globo')}})();
